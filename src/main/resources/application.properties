# Nombre identificador de tu aplicación Spring Boot.
spring.application.name=notification-api

# ---------- CONFIGURACIÓN DE LA BASE DE DATOS ----------
# La configuración siguiente permite a tu aplicación conectarse con una base de datos MySQL.

# → TU BASE LOCAL (Comentada porque actualmente no la usas)
#spring.datasource.url=jdbc:mysql://localhost:3306/notification?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=Chack689

# → TU BASE REMOTA (Configuración actual activa)
# URL para conectarse a una base MySQL remota ubicada en ies9021.edu.ar.
spring.datasource.url=jdbc:mysql://ies9021.edu.ar:3306/ies9021_coco?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Usuario para la conexión remota.
spring.datasource.username=ies9021_userdb
# Contraseña del usuario remoto.
spring.datasource.password=Xsw23edc.2025

# ---------- CONFIGURACIÓN JPA/HIBERNATE ----------
# Actualiza automáticamente el esquema de la base de datos según las entidades definidas en tu código Java.
# Opciones posibles: none, validate, update, create, create-drop.
spring.jpa.hibernate.ddl-auto=update

# Muestra en la consola todas las consultas SQL que realiza JPA/Hibernate.
spring.jpa.show-sql=true

# Especifica el dialecto específico para que Hibernate pueda generar consultas optimizadas para MySQL 8.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---------- CONFIGURACIÓN JWT ----------
# JWT (JSON Web Token) se usa para autenticación y autorización segura en tu API.

# Clave secreta usada para firmar y verificar los JWT. Es importante mantenerla segura y no compartirla públicamente.
jwt.secret=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxNDU3ODIzIiwibmFtZSI6IkVsaWFuIEd1ZXZhcmEiLCJpYXQiOjE1MTYyMzkwMjJ9.-WSbdyiDXTUgVV6EPFvae-IdRHSlwyQaBNFoBPOY9TY

# Tiempo de expiración del token JWT en milisegundos (3600000 ms = 1 hora).
jwt.expiration=3600000

# ---------- CONFIGURACIÓN DE LOGGING ----------
# Establece el nivel de log a DEBUG para Spring Security.
# Esto mostrará información detallada de seguridad en la consola, útil para depuración.
logging.level.org.springframework.security=DEBUG
